# BoxLang ORM

The [BoxLang ORM module](https://github.com/ortus-boxlang/bx-orm) allows your BoxLang
  application to integrate with the powerful [Hibernate ORM](https://hibernate.org/orm/). With Hibernate, you can interact with your database records in an object oriented fashion, using a BoxLang class to denote each record and simple getters and setters for each field value:

```js
class entityName="Auto" persistent="true" {

	property name="id" type="string" fieldtype="id" ormtype="string";
	property name="make" type="string";
	property name="model" type="string";

    function onPreInsert(){
        log.info( "Inserting new Auto: #getMake()# #getModel()#" );
    }
}
```

Install bx-orm from CommandBox for web runtimes:

```bash
# Using CommandBox to install for web servers.
box install bx-orm
```

Note that only the web runtime is currently supported.

## Contributed BIFs

* `EntityDelete()`
* `EntityLoad()`
* `EntityLoadByExample()`
* `EntityLoadByPK()`
* `EntityMerge()`
* `EntityNameArray()`
* `EntityNameList()`
* `EntityNew()`
* `EntityReload()`
* `EntitySave()`
* `EntityToQuery()`
* `ORMClearSession()`
* `ORMCloseAllSessions()`
* `ORMCloseSession()`
* `ORMEvictCollection()`
* `ORMEvictEntity()`
* `ORMEvictQueries()`
* `ORMExecuteQuery()`
* `ORMFlush()`
* `ORMFlushAll()`
* `ORMGetHibernateVersion()`
* `ORMGetSession()`
* `ORMGetSessionFactory()`
* `ORMReload()`

## GitHub Repository and Reporting Issues

Visit the [GitHub repository](https://github.com/ortus-boxlang/bx-orm) for release notes. You can also file a bug report or improvement suggestion  via [Jira](https://ortussolutions.atlassian.net/issues/?jql=project+%3D+%22BLMODULES%22+AND+component+%3D+bx-orm+ORDER+BY+created+DESC&atlOrigin=eyJpIjoiMzJhODRmODgxM2I5NDRiOTlmOThkNjIxMDY4ODJjZjkiLCJwIjoiaiJ9).
